{
  "name": "pi",
  "version": "0.1.2-beta.16",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@10.18.3",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "pnpm --filter @amaster.ai/pi-storage clean && pnpm --filter @amaster.ai/pi-storage prisma:generate && tsc -b packages/*/tsconfig.json",
    "pr-check": "pnpm build && pnpm test && pnpm typecheck",
    "typecheck": "pnpm --filter @amaster.ai/pi-storage prisma:generate && tsc -b packages/*/tsconfig.json --pretty false && tsc -p tsconfig.check.json --pretty false",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "vitest run",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@types/node": "^24.0.0",
    "husky": "^9.1.7",
    "jiti": "^2.7.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@google/genai",
      "@prisma/client",
      "@prisma/engines",
      "better-sqlite3",
      "esbuild",
      "ffmpeg-static",
      "koffi",
      "prisma",
      "protobufjs"
    ]
  }
}
